--- /tmp/OpenRadioss-src/engine/source/engine/resol.F.pre-av-dump.bak	2026-09-08 17:10:08.449953258 +0000
+++ /tmp/OpenRadioss-src/engine/source/engine/resol.F	2026-09-09 01:39:19.873990380 +0000
@@ -4931,6 +4931,21 @@
               IF (IMON>0) CALL STOPTIME(TIMERS,TIMER_ASM)
               CALL TRACE_OUT(15)
               CALL spmd_profile_end(PROF_ASM)
+C     web-mbd: host float64 F right after FORINT+ASSPAR (before RWALL/kin)
+              IF (ISPMD==0 .AND. NCYCLE<=9) THEN
+                WRITE(FILNAM,'(A,I1,A)') 'wmbd_postforint_',NCYCLE,'.f64bin'
+                OPEN(UNIT=777,FILE=TRIM(FILNAM),
+     .               ACCESS='STREAM',FORM='UNFORMATTED',STATUS='REPLACE')
+                WRITE(777) NCYCLE,NUMNOD,DT1,DT2,DT12
+                DO I=1,NUMNOD
+                  WRITE(777) NODES%ITAB(I),
+     .              NODES%A(1,I),NODES%A(2,I),NODES%A(3,I),
+     .              NODES%V(1,I),NODES%V(2,I),NODES%V(3,I),
+     .              NODES%X(1,I),NODES%X(2,I),NODES%X(3,I),
+     .              NODES%MS(I)
+                ENDDO
+                CLOSE(777)
+              ENDIF
 C========================================================================================
 C                                    NON PARALLEL SECTION (SMP)
 C========================================================================================
@@ -6895,6 +6910,21 @@
                   !NODES%FORCES(1:3,1:NUMNOD) = nodes%A(1:3,1:NUMNOD) - NODES%FORCES(1:3,1:NUMNOD) 
                   CALL COUPLING_SYNC(coupling,DT2,nodes,COUPLING_FORCES)
                 ENDIF
+C     web-mbd: host float64 F(=A pre-ACCELE),V,X for NCYCLE<=9
+              IF (ISPMD==0 .AND. NCYCLE<=9) THEN
+                WRITE(FILNAM,'(A,I1,A)') 'wmbd_preaccele_',NCYCLE,'.f64bin'
+                OPEN(UNIT=778,FILE=TRIM(FILNAM),
+     .               ACCESS='STREAM',FORM='UNFORMATTED',STATUS='REPLACE')
+                WRITE(778) NCYCLE,NUMNOD,DT1,DT2,DT12
+                DO I=1,NUMNOD
+                  WRITE(778) NODES%ITAB(I),
+     .              NODES%A(1,I),NODES%A(2,I),NODES%A(3,I),
+     .              NODES%V(1,I),NODES%V(2,I),NODES%V(3,I),
+     .              NODES%X(1,I),NODES%X(2,I),NODES%X(3,I),
+     .              NODES%MS(I)
+                ENDDO
+                CLOSE(778)
+              ENDIF
 C========================================================================================
 C                                     PARALLEL SECTION (SMP)
 C========================================================================================
@@ -6967,6 +6997,22 @@
                 call spmd_profile_end(PROF_ACCELE)
                 CALL PYTHON_END_OPENMP(PYTHON)
 
+C     web-mbd: host float64 A,V,X after ACCELE (pre-RGWALL) for NCYCLE<=9
+              IF (ISPMD==0 .AND. NCYCLE<=9) THEN
+                WRITE(FILNAM,'(A,I1,A)') 'wmbd_postaccele_',NCYCLE,'.f64bin'
+                OPEN(UNIT=779,FILE=TRIM(FILNAM),
+     .               ACCESS='STREAM',FORM='UNFORMATTED',STATUS='REPLACE')
+                WRITE(779) NCYCLE,NUMNOD,DT1,DT2,DT12
+                DO I=1,NUMNOD
+                  WRITE(779) NODES%ITAB(I),
+     .              NODES%A(1,I),NODES%A(2,I),NODES%A(3,I),
+     .              NODES%V(1,I),NODES%V(2,I),NODES%V(3,I),
+     .              NODES%X(1,I),NODES%X(2,I),NODES%X(3,I),
+     .              NODES%MS(I)
+                ENDDO
+                CLOSE(779)
+              ENDIF
+
 C------------------------------------------
 C       DT_DC for thick-shell
 C-----------------------------------------------
@@ -7703,6 +7749,22 @@
               end if
               CALL PYTHON_END_OPENMP(PYTHON)
 
+C     web-mbd: host float64 A,V,X after RGWALL for NCYCLE<=9
+              IF (ISPMD==0 .AND. NCYCLE<=9) THEN
+                WRITE(FILNAM,'(A,I1,A)') 'wmbd_postwall_',NCYCLE,'.f64bin'
+                OPEN(UNIT=780,FILE=TRIM(FILNAM),
+     .               ACCESS='STREAM',FORM='UNFORMATTED',STATUS='REPLACE')
+                WRITE(780) NCYCLE,NUMNOD,DT1,DT2,DT12
+                DO I=1,NUMNOD
+                  WRITE(780) NODES%ITAB(I),
+     .              NODES%A(1,I),NODES%A(2,I),NODES%A(3,I),
+     .              NODES%V(1,I),NODES%V(2,I),NODES%V(3,I),
+     .              NODES%X(1,I),NODES%X(2,I),NODES%X(3,I),
+     .              NODES%MS(I)
+                ENDDO
+                CLOSE(780)
+              ENDIF
+
               IF (IMON>0) CALL STOPTIME(TIMERS,TIMER_KIN)
               IF (IMONM > 0) CALL STOPTIME(TIMERS,43)
 
